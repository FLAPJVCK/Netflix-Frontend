import React from 'react';
import Logo from '../logo/Logo';
import './Footer.css'

const Footer = () => {
    return (
        <footer className='footer'>
            <Logo/>
        </footer>
    );
};

export default Footer;
